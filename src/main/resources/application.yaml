spring:
  application:
    name: WP-AI
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4.1-mini

wpai:
  embedding:
    model: text-embedding-3-small
